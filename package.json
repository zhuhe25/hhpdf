{
  "name": "pdf-export",
  "version": "1.0.0",
  "description": "Export any URL to PDF via CLI",
  "main": "index.js",
  "bin": {
    "pdf-export": "./index.js"
  },
  "scripts": {
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.55.1"
  }
}
